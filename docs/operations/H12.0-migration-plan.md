# H12.0 — Future Migration Plan (Proposed Order)

**Date:** 2026-09-17  
**Phase:** H12.0 — Audit only  
**Verdict input:** `GO_WITH_PERFORMANCE_WARNING` (H11)

**IMPORTANT:** This plan is **proposed only**. H12.1+ **not launched**. No execution in H12.0.

---

## Preconditions (all phases)

Before any Legacy/n8n reduction:

1. V2 equivalent exists and is production-verified
2. No remaining consumer on Legacy/n8n for that capability
3. Fallback policy updated with rollback documented
4. Observability proves zero usage for 7+ days (when metrics available)
5. Financial reconciliation gate passes

---

## Proposed Phase Order

### H12.1 — Instrumentation & Dead Code Documentation

| Item | Risk | Action |
|------|------|--------|
| Add `ashy_v2_request` structured log + optional metrics endpoint | LOW | Instrument only |
| Document `routes/messages.js` (unmounted) | LOW | Mark CANDIDATE |
| Document `HomePage.jsx` (unrouted n8n demo) | LOW | Mark CANDIDATE |
| `AIRTABLE_*` env vars | LOW | Mark CANDIDATE |

**No deletions in H12.1.**

---

### H12.2 — Frontend Read Path Alignment

| Item | Current | Target |
|------|---------|--------|
| `VITE_ASHY_READ_CHAT` | Build-time flag | Ensure ON in production build |
| READ routing | Ashy first, n8n fallback | Measure fallback rate |
| History/thread | Always n8n-backed API | Plan Supabase-only path |

**Blocked until:** fallback rate instrumented.

---

### H12.3 — Legacy READ Fallback Reduction

| Item | Risk | Prerequisite |
|------|------|--------------|
| Reduce n8n READ fallback | MEDIUM | H12.2 + 7d zero fallback logs |
| Legacy agent READ branch | MEDIUM | V2_PRIMARY strict mode tested in staging |
| `/history` n8n merge | MEDIUM | Supabase history completeness proven |

**Do not disable n8n Bot.**

---

### H12.4 — Legacy WRITE Path (Agent Branch)

| Item | Risk | Prerequisite |
|------|------|--------------|
| Legacy `create_expense`/`create_sale` non-F4 path | **HIGH** | Confirm all writes go F4-B2 only |
| Legacy agent passthrough when V2 unhandled | **HIGH** | V2 coverage 100% for active write intents |

**CRITICAL:** Financial write loss risk.

---

### H12.5 — n8n Write Workflows (Partial)

Migrate per capability (not whole Bot):

| Capability | n8n nodes | V2 needed | Risk |
|------------|-----------|-----------|------|
| Stock receipt | MAJ Stock, Créer Ligne Mvt | `adjust_stock` tool + F4 | HIGH |
| Product add/modify | Créer/Modifier Produit | Product write tools | HIGH |
| Payment | Régler Vente | Payment RPC + confirm | HIGH |
| Sale (prod) | Créer Vente | F4-B2 + catalog seed | MEDIUM |

**Order:** expense (done) → sale prod → stock → payment → product.

---

### H12.6 — PDF Migration

| Item | Current | Target |
|------|---------|--------|
| PDF generation | n8n HTML→PDF only | V2 report export OR dedicated API |
| Frontend routing | Always n8n (`chatRouter.js:166`) | Route to V2 when ready |

**Risk:** **CRITICAL** — user-facing reports.

---

### H12.7 — Final Fallback Reduction

| Item | Condition |
|------|-----------|
| Disable n8n chat webhook for READ | Zero fallback 30d + staging burn-in |
| Retire `POST /chat` route | All frontend traffic on `/api/ashy/chat` |
| Retire Ash Ledger Bot (partial) | Only after H12.5 + H12.6 complete |
| Subscription workflow | Independent; migrate last or keep |

---

## Capability Migration Status (H12.0 snapshot)

| Capability | Status |
|------------|--------|
| READ sales/expenses/stock/products/debts | **V2_READY** |
| ANALYSIS/COMPARE/SUMMARIZE | **V2_READY** |
| CREATE_EXPENSE | **V2_READY** (prod) |
| CREATE_SALE | **BLOCKED** (prod catalog) |
| UPDATE/ADJUST writes | **NOT_STARTED** |
| PDF | **NOT_STARTED** |
| History n8n merge | **AUDITED** |
| Subscription webhook | **UNKNOWN** usage frequency |
| Batch/admin n8n | **NOT_STARTED** |

---

## Rollback Strategy (for future phases)

Each phase must document:

1. Flag to revert (`ASHY_V2_*`, `VITE_ASHY_*`)
2. n8n workflow remains published (never delete first)
3. `/chat` route remains mounted until explicit H12.7
4. Financial reconciliation gate before/after

---

**STOP — H12.1 not launched.**
