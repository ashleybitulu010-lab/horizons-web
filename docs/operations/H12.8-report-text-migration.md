# H12.8 — REPORT TEXT MIGRATION

**Date:** 2026-09-18  
**Prior:** H12.7 `H12.7_AUDIT_ONLY` @ `b8276ac`  
**Verdict:** **`H12.8_PENDING_GATE`** (local tests PASS; production gate requires manual approval)

---

## 1. Verdict

**Implementation complete locally.** Production gate **not executed** (requires SSH + credentials approval).

| Check | Status |
|-------|--------|
| Report text → V2 (frontend routing) | ✅ Local tests |
| PDF → n8n (PDF guard prioritaire) | ✅ Local tests |
| Classifier GENERAL SUMMARIZE | ✅ Backend tests |
| Profit/Analysis regression | ✅ Local tests |
| Financial delta Δ0 | ⏳ Pending prod gate |
| Production smoke | ⏳ Pending prod gate |

**Expected verdict after gate:** `H12.8_GO_WITH_GAPS`

---

## 2. Audit

See `docs/operations/H12.8-report-text-audit.md`.

---

## 3. Report vs PDF

| Type | Example | Route |
|------|---------|-------|
| Report texte | « Fais-moi un bilan » | **ASHY V2** (flag ON) |
| Report financier | « Résumé financier » | **ASHY V2** |
| PDF | « Fais-moi un rapport PDF » | **n8n** (always, PDF guard first) |
| PDF standalone | « Envoie-moi le PDF » | **n8n** (enhanced guard) |

**PDF_N8N = ACTIVE** — unchanged.

---

## 4. Classification

V2 goal contract: **`GENERAL / SUMMARIZE / ANALYSIS`** — no new domain created.

| Phrase | Frontend (H128 ON) | V2 classifier |
|--------|-------------------|---------------|
| Fais-moi un rapport | ASHY | GENERAL SUMMARIZE |
| Fais-moi un bilan | ASHY | GENERAL SUMMARIZE |
| Résumé financier | ASHY | GENERAL SUMMARIZE |
| Quel est mon bénéfice ? | ASHY (PROFIT first) | PROFIT RETRIEVE |
| Analyse mes résultats | ASHY (PROFIT first) | GENERAL SUMMARIZE |
| Fais-moi un rapport PDF | **n8n** | — (frontend guard) |

---

## 5. Routing

Order in `resolveChatRoute`:

1. PDF guard (`isPdfReportRequest`)
2. WRITE guards
3. Sales → Expenses → Stock → Products → Debts → Profit
4. **Report** (NEW)
5. Global read
6. Legacy/n8n

---

## 6. Feature flag

| Flag | Default repo | Production (post-gate) |
|------|--------------|------------------------|
| `VITE_ASHY_READ_REPORT` | **`false`** | **`true`** (after gate GO) |

Rollback: `VITE_ASHY_READ_REPORT=false` → `node tools/_h128_deploy_frontend.mjs`

Other flags unchanged.

---

## 7. Backend changes (minimal)

**File:** `apps/api/src/agent/intelligence-v2/goal-classifier-rules.js`

- Added `isReportTextQuery()` for bilan/rapport/récap phrases
- Merged into `isGeneralActivitySummaryQuery()` → `GENERAL SUMMARIZE`
- No changes to `generate_report`, financial-analyzer, F4-B2, or WRITE paths

---

## 8. Frontend changes

**Files:**

- `src/lib/ashyReadMigration.js` — `READ_CAPABILITY.REPORT`, `isReportReadIntent()`, `isAshyReadReportMigrated()`
- `src/lib/chatRouter.js` — REPORT hook after PROFIT; PDF guard enhanced (`génère`, standalone PDF)
- `.env.example` — `VITE_ASHY_READ_REPORT=false`

**Not modified:** `pdfDownload.js`, n8n PDF workflow, PDF transport.

---

## 9. Tests

### Local results (PASS)

| Suite | Pass | Fail |
|-------|------|------|
| `ashyReadMigration.test.js` | 52 | 0 |
| `chatRouter.test.js` | 32 | 0 |
| `intelligence-v2-classifier-h128.test.js` | 14 | 0 |
| `intelligence-v2-classifier-h126.test.js` | 17 | 0 |

**H12.8 coverage:**

- 5 report formulations → V2
- 4 PDF formulations → n8n (guard)
- Profit/Analysis regression
- Sales/Expenses/Stock/Products/Debts regression
- WRITE safety

---

## 10. Production smoke

**Pending.** Run manually:

```bash
node tools/_h128_run_gate.mjs
```

Gate script: `tools/_h128_report_text_migration_gate.mjs`

Deploy scripts:

- `tools/_h128_deploy_api.mjs`
- `tools/_h128_deploy_frontend.mjs`

---

## 11. Metrics (expected)

| Metric | Expected delta |
|--------|----------------|
| `READ_TOTAL` | +N (report reads) |
| `READ_V2_TOTAL` | +N |
| `REPORT.READ_V2_SUCCESS` | +N (GENERAL SUMMARIZE maps to REPORT capability) |
| `ACTION_TO_N8N_FALLBACK` | **0** |

---

## 12. Financial delta

Expected production: **Δ0** on all tables including `agent_write_operations`.

---

## 13. Scope / security

- Auth required (`Bearer` token)
- Activity scope from backend session — no `clientId`/`activityId` from message
- Foreign activity → **403** (gate test included)

---

## 14. PDF guard

Enhanced `isPdfReportRequest`:

- `\bpdf\b` + export cue (`génère`, `envoie-moi`, etc.) → n8n
- bilan/rapport + export cue → n8n (unchanged)

Verified: Report flag ON + PDF phrase → **never** V2 Report.

---

## 15. n8n / Legacy status

| Component | Status |
|-----------|--------|
| n8n PDF pipeline | **ACTIVE** — unchanged |
| n8n text report (fallback) | Available when `VITE_ASHY_READ_REPORT=false` |
| Legacy orchestrator | **ACTIVE** |

---

## 16. Rollback

```bash
VITE_ASHY_READ_REPORT=false
node tools/_h128_deploy_frontend.mjs
```

PDF requests remain n8n regardless of rollback state.

---

## 17. Known gaps

1. GENERAL SUMMARIZE metrics counted under `REPORT` capability (same as H12.6)
2. Direct `/api/ashy/chat` with PDF phrase may misclassify if frontend guard bypassed
3. n8n PDF numbers still not aligned with `FinancialAnalysisResult` (H12.8 out of scope)

---

## 18. Git

**Not committed** — awaiting production gate GO.

Suggested commit after gate:

```
feat(operations): migrate report text to Ashy V2 (H12.8)
```

---

## 19. Next step

**STOP** — await:

1. Manual production gate approval + run
2. Human validation
3. Commit + push if `H12.8_GO`

**Do NOT start:**

- PDF generation service (H13)
- WRITE migrations
- n8n cleanup / Legacy removal

---

*H12.8 = REPORT TEXTE ONLY. PDF stays n8n. No writes.*
