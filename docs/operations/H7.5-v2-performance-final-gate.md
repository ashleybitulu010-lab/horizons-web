# H7.5 V2 Performance Final Gate

**Date:** 2026-09-17  
**Phase:** H7.5 — Final V2 performance & cutover readiness (NOT cutover)  
**Prior:** H7.4 READY_WITH_PERFORMANCE_WARNING

---

## 1. Verdict

**READY_WITH_PERFORMANCE_WARNING**

Production safety from H7.4 **holds**. V2 **code is not deployed** on the production VPS container (commit `6097852`). **No safe internal V2 benchmark on VPS** is possible without deploying V2 or activating production flags. Staging V2 reference benchmark **PASS** (p50 ~2.1 s, 5 samples).

**Cutover NOT authorized.** V2 NOT activated.

---

## 2. H7.4 baseline (extracted)

| Area | H7.4 result |
|------|-------------|
| Verdict | READY_WITH_PERFORMANCE_WARNING |
| Financial integrity | PASS — all financial deltas 0 |
| Stock chat | PASS — « Aucun stock enregistré pour le moment. » |
| Legacy p50 / p95 | **2618 ms / 4686 ms** (9 samples, `/chat` n8n) |
| V2 VPS benchmark | NOT SIMULATED |
| Production flags | All ABS/OFF |
| Tests | 1220 PASS / 0 FAIL / 10 SKIP |
| NOT SIMULATED | V2 VPS internal benchmark, Supabase status, agent_sessions count |
| Recommendation | Staging V2 perf + define VPS canary path before cutover |

---

## 3. Benchmark classification

| Tool / mechanism | Class | Rationale |
|------------------|-------|-----------|
| `tools/_ashy_v2_phase_h7_perf_baseline.mjs` | **STAGING_ONLY** | Blocks production Supabase; local HTTP server; sets V2+PRIMARY in **process** only; real V2 pipeline via `/api/ashy/chat`; cleans up test rows |
| `tools/_ashy_v2_phase_h5_shadow_gate.mjs` | **STAGING_ONLY** | Production blocked; shadow mode on staging |
| `tools/_ashy_v2_phase_h7_staging_smoke.mjs` | **STAGING_ONLY** | Full staging smoke, not perf |
| `tools/_ashy_v2_parallel_benchmark.mjs` | **LEGACY_ONLY** | Mock delays; scheduler unit benchmark, not V2 e2e |
| `tools/_ashy_v2_orchestrator_harness.mjs` | **LEGACY_ONLY** | Mock `executeTool`; no real Supabase |
| `tools/_ashy_v2_response_harness.mjs` | **LEGACY_ONLY** | Golden mock step results |
| `docker exec` V2 on VPS (hypothetical) | **UNSAFE / N/A** | Production container ** lacks `intelligence-v2/`** — V2 modules absent at deployed commit |
| Production HTTP `/api/ashy/chat` with flags | **PRODUCTION_HTTP_V2** | **Interdit H7.5** — would expose V2 to users |

**No SAFE_INTERNAL_V2 path on production VPS** at current deploy state.

---

## 4. V2 VPS benchmark

**NOT SIMULATED — no safe internal V2 execution path available on production VPS.**

Evidence (read-only SSH):

```
docker exec ash-ledger-api-v2-ash-ledger-api-1 ls /app/src/agent/intelligence-v2
→ No such file or directory
```

Deployed agent modules: `contract.js`, `index.js`, `intent-resolver/`, etc. — **pre-V2 tree**.

Production flags remain ABS/OFF (unchanged). Main API process unaffected.

### Staging V2 reference (STAGING_ONLY — not VPS)

Executed: `node --env-file=apps/api/.env.staging tools/_ashy_v2_phase_h7_perf_baseline.mjs`

| # | Domain | Duration (ms) | Result |
|---|--------|---------------:|--------|
| 1 | sales (READ) | 2031 | success |
| 2 | sales (READ) | 2037 | success |
| 3 | sales (READ) | 2084 | success |
| 4 | sales (READ) | 2924 | success |
| 5 | sales (READ) | 3240 | success |

Message: `"Combien ai-je vendu ce mois-ci ?"` — all samples `v2Http.handled`.

### Statistics (staging V2)

| Metric | Value |
|--------|-------|
| min | 2031 ms |
| max | 3240 ms |
| mean | 2463 ms |
| **p50** | **2084 ms** |
| p95 | 3240 ms (n=5) |

Note: local single-process server; staging Supabase; not identical to VPS production path.

No pipeline decomposition available from harness (global duration only).

---

## 5. Performance comparison

### Legacy production (H7.4 — unchanged)

| Metric | Value |
|--------|-------|
| Route | `POST /hcgi/api/chat` (n8n, V2 OFF) |
| p50 | 2618 ms |
| p95 | 4686 ms |
| Classification | p50 GREEN; p95 WARNING |

### Staging V2 (H7.5 reference)

| Metric | Value |
|--------|-------|
| p50 | 2084 ms |
| p95 | 3240 ms |
| Classification | **GOOD** (< 3 s p50) |

### VPS V2

**NOT SIMULATED** — V2 code absent on production container.

| Reference | p50 |
|-----------|-----|
| H6 outlier | ~7610 ms |
| H7 staging | ~2870–3228 ms |
| H7.3 VPS legacy | ~4370 ms |
| H7.4 VPS legacy | ~2618 ms |
| H7.5 staging V2 | ~2084 ms |

**Do not conflate legacy production latency with V2 latency.**

---

## 6. Financial integrity

H7.5 did **not** re-run production financial smokes (no new production traffic).

Staging perf baseline creates ephemeral test user/client on **staging only**; rows deleted after run. **No production financial impact.**

| Production financial delta (H7.4, still valid) | Value |
|--------------------------------------------------|-------|
| ventes | 0 |
| depenses | 0 |
| stocks | 0 |
| produits | 0 |
| agent_write_operations | 0 |

---

## 7. Logs

**Staging perf:** agent-session parity WARNINGs (expected in harness); no ERROR/Unhandled/timeouts on samples.

**Production VPS:** not re-scanned (no new production requests in H7.5).

---

## 8. Production flags

**PASS** — re-verified via `_ashy_v2_phase_h7_prod_flag_audit.sh`:

| Flag | State |
|------|-------|
| ASHY_INTELLIGENCE_V2 | ABS |
| ASHY_INTELLIGENCE_V2_HTTP | ABS |
| ASHY_INTELLIGENCE_V2_PRIMARY | ABS |
| ASHY_INTELLIGENCE_V2_ACTIONS | ABS |
| ASHY_INTELLIGENCE_V2_HTTP_CONFIRM | ABS |
| ASHY_INTELLIGENCE_V2_SHADOW | ABS |

No modification performed.

---

## 9. Tests

**PASS**

```
npm test: 1220 PASS / 0 FAIL / 10 SKIP
eslint intelligence-v2: PASS
```

No regression vs H7.4.

---

## 10. Risks

1. **V2 not deployed on VPS** — cutover impossible until V2 code ships to production container (currently `6097852` pre-V2).
2. **V2 VPS perf unknown** — staging suggests GOOD latency but VPS path unverified.
3. **Deploy gap** — local workspace has `intelligence-v2/` (uncommitted); production container does not.
4. **Legacy p95 ~4.7 s** — acceptable but monitor post-cutover.

---

## 11. NOT SIMULATED

- V2 VPS internal benchmark (no V2 code on container)
- Production financial before/after (H7.4 snapshot still authoritative)
- Supabase `/api/supabase/status`
- V2 pipeline decomposition (classifier/planner/etc.)

---

## 12. Cutover prerequisites

| Prerequisite | Status |
|--------------|--------|
| H7.2–H7.4 production safety | PASS |
| Financial integrity | PASS (H7.4) |
| Legacy latency characterized | PASS |
| **V2 code deployed to VPS** | **FAIL — absent** |
| **V2 VPS perf validated** | **NOT SIMULATED** |
| Staging V2 perf | PASS (reference) |
| Production flags OFF | PASS |
| Tests | PASS |

**Before cutover:** deploy V2 codebase to VPS → re-run SAFE_INTERNAL_V2 benchmark → then controlled flag activation in cutover phase.

---

## 13. Final decision

| Decision | **READY_WITH_PERFORMANCE_WARNING** |
|----------|-------------------------------------|
| Rationale | All H7.4 safety checks remain valid; staging V2 perf GOOD; but **V2 absent on production VPS** — cannot confirm VPS V2 latency or readiness for controlled cutover. |
| vs H7.4 | Added staging V2 reference; confirmed deploy gap on VPS |
| Cutover | **NOT authorized** |
| V2 activation | **NOT done** |
| Commit | **None** |

**READY_FOR_CUTOVER** requires V2 benchmark on VPS PASS — blocked until V2 code is deployed without activating user-facing flags.

---

*Generated: H7.5 — V2 performance final gate (read-only production inspection + staging reference).*
