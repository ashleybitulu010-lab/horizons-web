# H7.4 Final Production Safety + Performance Gate

**Date:** 2026-09-17  
**Phase:** H7.4 — Financial integrity + VPS performance (NOT cutover)  
**Prior:** H7.3 READY_WITH_PERFORMANCE_WARNING  
**Test account:** `ketura870@gmail.com` (credentials in secure env only)

---

## 1. Verdict

**READY_WITH_PERFORMANCE_WARNING**

Financial integrity **confirmed** — no financial table changed during READ smoke. Stock chat **functionally correct**. Legacy latency **improved** (p50 ~2.6 s). V2 VPS benchmark remains **NOT SIMULATED** (no safe path without activating production V2).

**Cutover NOT authorized.** Gate verification only.

---

## 2. Production flags

**PASS** — all ABS/OFF on VPS (`_ashy_v2_phase_h7_prod_flag_audit.sh`)

| Flag | State |
|------|-------|
| ASHY_INTELLIGENCE_V2 | ABS |
| ASHY_INTELLIGENCE_V2_HTTP | ABS |
| ASHY_INTELLIGENCE_V2_PRIMARY | ABS |
| ASHY_INTELLIGENCE_V2_ACTIONS | ABS |
| ASHY_INTELLIGENCE_V2_HTTP_CONFIRM | ABS |
| ASHY_INTELLIGENCE_V2_SHADOW | ABS |
| ASHY_V2_CUTOVER_MODE | ABS |
| ASHY_V2_PRIMARY_SAFE_FALLBACK | ABS |

No PRIMARY/HTTP/ACTIONS/CONFIRM ON detected.

---

## 3. Financial BEFORE snapshot

| Table | Count | Notes |
|-------|-------|-------|
| ventes | 0 | OK |
| depenses | 2 | pre-existing |
| stocks | 0 | fixed query (`*` count; no `id` column) |
| produits | 0 | OK |
| agent_write_operations | 1 | pre-existing |
| chat_messages | 66 | baseline |
| agent_sessions | N/A | count query failed (schema) |

**Stocks diagnostic (H7.3 root cause):** table `public.stocks` has **no `id` column**. Head-count on `id` returned error `"column stocks.id does not exist"`. Correct READ-only count: `select('*', { count: 'exact' }).limit(0)` → **0 rows**.

---

## 4. Production READ smoke

| Test | HTTP | Result |
|------|------|--------|
| Auth (PocketBase) | 200 | PASS |
| GET /api/me | 200 | PASS |
| Foreign activity | 403 | PASS |
| Chat READ stock | 200 | PASS |
| POST /history | 200 | PASS (70 messages) |
| Financial READs (sales/expenses/products/debts/report) | 200 | PASS |

Route: legacy `POST /hcgi/api/chat` (n8n path). V2 OFF.

---

## 5. Financial AFTER snapshot

| Table | Count |
|-------|-------|
| ventes | 0 |
| depenses | 2 |
| stocks | 0 |
| produits | 0 |
| agent_write_operations | 1 |
| chat_messages | 86 |

---

## 6. Before/After delta

| Table | BEFORE | AFTER | DELTA | Expected |
|-------|--------|-------|-------|----------|
| ventes | 0 | 0 | **0** | 0 |
| depenses | 2 | 2 | **0** | 0 |
| stocks | 0 | 0 | **0** | 0 |
| produits | 0 | 0 | **0** | 0 |
| agent_write_operations | 1 | 1 | **0** | 0 |

**No financial write detected during H7.4.**

---

## 7. Chat persistence delta

| Table | BEFORE | AFTER | DELTA |
|-------|--------|-------|-------|
| chat_messages | 66 | 86 | **+20** |

Expected — READ chat smoke creates user/assistant messages. **Not a financial write.**

---

## 8. Stock chat response validation

| Field | Value |
|-------|-------|
| Message | `"Quel est mon stock ?"` |
| HTTP | 200 |
| Reply | `"Aucun stock enregistré pour le moment."` |
| Length | 38 chars |
| Direct READ compare | identical reply (38 chars) |
| Classification | **A — short but correct** |

H7.3's ~19 char reply was likely truncated in reporting or transient. H7.4 confirms functional empty-stock response, consistent across two phrasings. No formatter bug observed. Supabase stock rows: **0** (matches reply).

---

## 9. Legacy live latency

**Label:** LEGACY `/chat` (n8n path, V2 OFF) — **NOT V2 latency**

| Metric | H7.4 | H7.3 | H7 staging | H6 |
|--------|------|------|------------|-----|
| p50 | **2618 ms** | 4370 ms | ~2870–3228 ms | ~7610 ms |
| p95 | **4686 ms** | 6754 ms | — | — |
| mean | 2823 ms | 4512 ms | ~3183 ms | — |
| min | 2247 ms | 2821 ms | — | — |
| max | 4686 ms | 6754 ms | — | — |
| samples | 9 | 6 | 5 | — |

Sample ms: 2704, 3022, 4686, 2392, 2618, 3067, 2274, 2247, 2399

**Classification:** p50 **GREEN** (aligned with H7 staging). p95 **WARNING** (notable but stable, no timeouts).

---

## 10. V2 VPS benchmark

**NOT SIMULATED**

`tools/_ashy_v2_phase_h7_perf_baseline.mjs` is **staging-only** (blocks production Supabase ref). No safe internal V2 benchmark on VPS without activating production V2 flags.

---

## 11. Performance analysis

| Question | Answer |
|----------|--------|
| A. Financial READs without write? | **Yes** — all financial deltas 0 |
| B. Latency acceptable? | **Legacy p50 yes; p95 borderline WARNING** |
| C. Stock response correct? | **Yes** — empty stock message consistent |
| D. Critical cutover blocker? | **No** — no auth/scope/financial failure |

Latency improved ~40% vs H7.3 (cold/warm mix). No timeouts or 500s during smoke.

---

## 12. Logs

**PASS** — API container (5 min window): auth OK lines only for `/me`, `/chat`, `/history`. No ERROR, Unhandled, timeout, or scope failures in filtered tail.

---

## 13. Supabase

**NOT SIMULATED** — `ASH_INTERNAL_HEALTH_KEY` unavailable. Non-blocking per H7 matrix.

---

## 14. n8n

**PASS** — Ash Ledger Bot + Subscription active. No modifications. READ chat triggered expected paths only.

---

## 15. Tests

**PASS**

```
npm test: 1220 PASS / 0 FAIL / 10 SKIP
eslint src/agent/intelligence-v2: PASS
```

No regression vs H7.3.

---

## 16. Risks

1. **V2 VPS perf unknown** — cutover perf validation incomplete until safe V2 benchmark or staging parity confirmed.
2. **Legacy p95 ~4.7 s** — acceptable for chat but monitor under load.
3. **Stocks count instrumentation** — use `*` not `id` for counts (schema has no `id` on `stocks`).
4. **Pre-existing depenses (2) + agent_write_operations (1)** — unchanged, not introduced by smoke.

---

## 17. NOT SIMULATED

- V2 VPS internal benchmark
- Supabase `/api/supabase/status`
- agent_sessions count (schema/query TBD)

---

## 18. Cutover prerequisites

| Prerequisite | H7.4 status |
|--------------|-------------|
| VPS verified (H7.2) | PASS |
| Flags OFF | PASS |
| Auth/scope/chat/history | PASS |
| Financial integrity | PASS |
| Stock READ functional | PASS |
| Legacy latency characterized | PASS (WARNING p95) |
| V2 VPS perf | NOT SIMULATED |
| V2 activation | **NOT done** |

---

## 19. Final decision

| Decision | **READY_WITH_PERFORMANCE_WARNING** |
|----------|-------------------------------------|
| Rationale | All critical safety checks PASS; financial deltas zero; stock OK; legacy perf improved. V2 VPS benchmark still NOT SIMULATED. |
| vs H7.3 | Performance warning downgraded in severity (p50/p95 improved); financial integrity now fully verified. |
| Cutover | **NOT authorized** |
| Commit | **None** |

**Recommendation:** Proceed to cutover **planning** only. Before H8/V2 activation: run staging V2 perf baseline + define V2 VPS canary benchmark path. Do **not** activate V2 on production until explicit cutover phase.

---

*Generated: H7.4 — read-only production safety + performance gate.*
