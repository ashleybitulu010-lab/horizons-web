# H12.1.1 — READ Routing Observability

**Date:** 2026-09-18  
**Phase:** H12.1.1 — READ routing observability (no migration)  
**Prior:** H12.1 `H12.1_GO` @ `65c39fb`  
**Gate script:** `tools/_h1211_read_routing_gate.mjs`  
**Deploy script:** `tools/_h1211_deploy_api.mjs`  
**Verdict:** **H12.1.1_GO_WITH_GAPS** (local validation PASS; production metrics endpoint pending deploy + gate password)

---

## 1. Objective

Improve observability of READ routing paths with proof of:

- V2 vs Legacy vs n8n vs SAFE_FALLBACK counts
- Per-capability breakdown (SALES, EXPENSES, STOCK, …)
- V2 error counts
- Latency aggregates (count, min, p50, p95, max)
- Correlation IDs linking V2 → fallback → n8n

**Observability only.** No READ migration, no flag changes, no writes.

---

## 2. Existing Observability (H11 / H12.0 / H12.1)

| Source | What existed | Gap |
|--------|--------------|-----|
| H11 `action-observability.js` | ACTION counters + `ashy_v2_action` logs | READ-specific routing absent |
| H11 gap report | `N8N_FALLBACK_TOTAL` not centrally counted | Addressed H12.1.1 |
| H12.1 SALES migration | Frontend routes SALES → V2 | No server-side READ path counter |
| `v2Http` response fields | Per-request goalType/domain | Not aggregated |
| `chat.js` n8n logs | Status + truncated body | No capability/outcome taxonomy |
| Frontend `chatTransport` | Console `v2_read_fallback_n8n` | Client-only, not aggregated |

**Decision:** Extend existing in-process counter pattern (`action-observability.js`) — no parallel system.

---

## 3. Architecture

```
POST /api/ashy/chat (V2)
  → create/reuse X-Ash-Read-Correlation
  → agent.run → v2Http goalType/domain
  → recordV2AshyChatRouting (READ only, skips ACTION)
  → response header X-Ash-Read-Correlation (not in JSON body)

POST /chat (Legacy/n8n)
  → reads X-Ash-Read-Correlation + X-Ash-Read-Fallback
  → isLikelyReadMessage OR safe-fallback header
  → recordLegacyN8nChatRouting (READ_LEGACY | READ_SAFE_FALLBACK)

GET /api/read-routing/metrics (internal key)
  → getReadRoutingMetricsSnapshot()
```

Frontend (`chatTransport.js`) generates correlation id per request, passes it to Ashy and n8n fallback via internal headers only — **not shown in Chat UI**.

---

## 4. Routing Events

Normalized internal event (logged as `ashy_read_routing`):

```json
{
  "event": "ashy_read_routing",
  "route": "READ",
  "capability": "SALES",
  "primaryPath": "V2_HTTP",
  "outcome": "READ_V2_SUCCESS",
  "fallback": false,
  "latencyMs": 1115,
  "correlationId": "rr-a1b2c3d4e5f6",
  "goalType": "QUESTION",
  "httpStatus": 200,
  "errorCode": null
}
```

**Never logged:** password, Bearer token, API keys, service_role, raw user UUIDs, full user messages.

Files: `apps/api/src/observability/read-routing-observability.js`, `read-capability-inference.js`.

---

## 5. Metrics

### Global counters

| Counter | Description |
|---------|-------------|
| `READ_TOTAL` | All recorded READ routing events |
| `READ_V2_TOTAL` | V2 success + error |
| `READ_LEGACY_TOTAL` | Direct n8n READ |
| `READ_N8N_FALLBACK_TOTAL` | Safe fallback + legacy n8n |
| `READ_V2_ERROR_TOTAL` | V2 failures |
| `READ_SAFE_FALLBACK_TOTAL` | V2 → n8n fallback |
| `ACTION_TO_N8N_FALLBACK` | Invariant counter (must stay 0) |

### Per capability

SALES, EXPENSES, STOCK, PRODUCTS, DEBTS, PROFIT, REPORT, HISTORY, UNKNOWN — each with outcome sub-counters.

Export: `GET /api/read-routing/metrics` (requires `x-ash-internal-key`).

---

## 6. Latency

Per-capability rolling window (max 512 samples):

- count, min, p50, p95, max

**Not implemented:** External histogram / Prometheus export (same gap as H11).

Single-sample p50 is not reported as meaningful in gate scripts.

---

## 7. Fallback

| Path | Outcome | Instrumented |
|------|---------|--------------|
| V2 success | `READ_V2_SUCCESS` | Yes |
| V2 error | `READ_V2_ERROR` | Yes |
| V2 fail → n8n | `READ_SAFE_FALLBACK` | Yes (via `X-Ash-Read-Fallback: safe-fallback`) |
| Legacy primary n8n | `READ_LEGACY` | Yes |
| ACTION → n8n | **Blocked** | `ACTION_TO_N8N_FALLBACK` incremented, event dropped |

Unit test: `ACTION cannot fallback to n8n — blocked and counted`.

---

## 8. Sales

H12.1 migrated SALES reads to V2 frontend routing. H12.1.1 records:

- `goalDomain=SALES` → capability `SALES`
- Outcome `READ_V2_SUCCESS` when `executionCode=SUCCESS`
- Correlation header on `/api/ashy/chat` response

Expected production signal after deploy: `byCapability.SALES.READ_V2_SUCCESS >= 1` per sales READ smoke.

---

## 9. Expenses

**Not migrated.** Observation only.

Message: `Quelles sont mes dépenses ?`

| Layer | Expected path (H12.1 baseline) |
|-------|------------------------------|
| Frontend | n8n (global read flag OFF, not SALES) |
| Server metric | `READ_LEGACY` or `READ_V2_*` if user has read flag ON |

H12.1.1 does **not** change routing.

---

## 10. Stock

Capability inference: `/stock/i`, `catalogue`.  
Instrumented when message matches READ patterns on either path.

---

## 11. Products

Capability inference: `/produits/i`.  
Instrumented on V2 (`goalDomain=PRODUCTS`) or Legacy n8n.

---

## 12. Debts

Capability inference: `dette`, `me doit`, `impay`.  
V2 domain `DEBTS` mapped via `mapGoalDomainToCapability`.

---

## 13. Profit

Capability inference: `bénéfice`, `profit`, `marge`.  
V2 domain `PROFIT`.

---

## 14. Report

Capability inference: `bilan`, `rapport`, `synthèse`, `fais-moi le point`, `compare`.  
V2 `GENERAL` domain → capability `REPORT`.

---

## 15. History

Capability inference: `historique`.  
History merge via n8n (`n8n history` log) — separate from chat READ routing; **NOT_INSTRUMENTED** in READ routing counters unless message hits `/chat` with history pattern.

---

## 16. n8n

Legacy `/chat` proxy records READ events when:

- `isLikelyReadMessage(message)` OR
- `X-Ash-Read-Fallback: safe-fallback`

Does **not** count n8n workflows that exist but are not executed.

Correlation id propagated from V2 attempt on safe fallback.

---

## 17. Legacy

Direct n8n route (`chatRoute !== ASHY`) → `READ_LEGACY` when message is READ-shaped.

Global read flag OFF: most non-SALES READs remain Legacy primary.

---

## 18. Security

| Check | Result |
|-------|--------|
| Logs exclude tokens/passwords/messages | Unit test PASS |
| Metrics snapshot excludes secrets | Sanitizer applied |
| Foreign activity | 403 (gate pattern from H12.1) |
| Public JSON contract | Unchanged |
| Internal headers | `X-Ash-Read-Correlation`, `X-Ash-Read-Fallback` — not in Chat UI |

---

## 19. Financial Integrity

Gate pattern (same as H12.1):

| Table | Expected delta |
|-------|----------------|
| ventes | 0 |
| depenses | 0 |
| stocks | 0 |
| produits | 0 |
| agent_write_operations | 0 |

READ-only smoke; no writes triggered by observability changes.

---

## 20. Production Validation

### Local tests (2026-09-18)

| Suite | Result |
|-------|--------|
| `read-routing-observability.test.js` | **10/0** |
| `chatTransport.test.js` | **17/0** |
| Full API suite | **1365/0** (10 skipped) |

### Production gate

Run after API deploy:

```powershell
$env:ASH_TEST_PASSWORD = '<secret>'
node tools/_h1211_read_routing_gate.mjs
```

Artifact: `%TEMP%/h1211_report.json`

**Pending:** API deploy (`node tools/_h1211_deploy_api.mjs`) + gate with credentials.

---

## 21. Gaps

| Gap | Status |
|-----|--------|
| External metrics export (Prometheus/Datadog) | NOT_IMPLEMENTED |
| n8n execution correlation (workflow run ID) | NOT_IMPLEMENTED |
| HISTORY via n8n-history merge | NOT_INSTRUMENTED in READ counters |
| Production metrics snapshot | Pending deploy + gate |
| Histogram beyond in-process p50/p95 | NOT_IMPLEMENTED (documented) |

---

## 22. Risks

| Risk | Mitigation |
|------|------------|
| In-process counters reset on container restart | Accept for phase; logs persist in container stdout |
| Message-based capability inference false positives | WRITE openers excluded; UNKNOWN fallback |
| Correlation only on instrumented paths | Documented; HISTORY gap noted |

---

## 23. Next Migration Candidate

Per H12.1 plan (unchanged): **EXPENSES READ** — after observability gap closed.

**Do not start H12.2.** Observability phase complete pending production gate PASS.

---

## H1211_READ_ROUTING_METRICS (template)

| Capability | V2 | Legacy | n8n fallback | errors | latency p50 | confidence |
|------------|-----|--------|--------------|--------|-------------|------------|
| SALES | gate | — | — | gate | gate | HIGH after gate |
| EXPENSES | gate | gate | — | gate | gate | MEDIUM |
| STOCK | gate | gate | — | gate | gate | MEDIUM |
| PRODUCTS | gate | gate | — | gate | gate | MEDIUM |
| DEBTS | gate | gate | — | gate | gate | MEDIUM |
| PROFIT | gate | gate | — | gate | gate | MEDIUM |
| REPORT | gate | gate | — | gate | gate | MEDIUM |
| HISTORY | NOT_INSTRUMENTED | — | — | — | — | LOW |

Evidence window: gate tag `h1211-*`, financial delta 0.

---

## Files Changed

| File | Change |
|------|--------|
| `apps/api/src/observability/read-capability-inference.js` | New |
| `apps/api/src/observability/read-routing-observability.js` | New |
| `apps/api/src/routes/api/read-routing-metrics.js` | New |
| `apps/api/src/routes/api/index.js` | Metrics route |
| `apps/api/src/routes/api/ashy-chat.js` | V2 READ instrumentation |
| `apps/api/src/routes/chat.js` | Legacy/n8n instrumentation |
| `apps/api/tests/read-routing-observability.test.js` | 10 unit tests |
| `src/lib/chatTransport.js` | Correlation headers on fallback |
| `src/lib/ashyChatApi.js` | Correlation header pass-through |
| `src/lib/n8nChatApi.js` | Fallback header pass-through |
| `tools/_h1211_read_routing_gate.mjs` | Production gate |
| `tools/_h1211_deploy_api.mjs` | API deploy |
