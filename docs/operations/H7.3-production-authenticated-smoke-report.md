# H7.3 Production Authenticated Smoke Gate

**Date:** 2026-09-16  
**Test account:** `ketura870@gmail.com`  
**Phase:** H7.3 — Authenticated production verification (NOT cutover)  
**V2 production:** OFF (unchanged)

---

## 1. Verdict

**READY_WITH_PERFORMANCE_WARNING**

All critical authenticated smokes **PASS**. Financial counter before/after not fully captured (VPS Node WebSocket issue on first pass; post-smoke snapshot only). Legacy chat p95 ~6.8 s (above H7 staging ~3.2 s baseline).

**NOT cutover authorized.** Verification gate only.

---

## 2. Authentication

**PASS**

```
ASH_TEST_PASSWORD: SET (process env, not logged)
PocketBase login: HTTP 200
```

No password, JWT, or token stored in this report or repository.

---

## 3. /api/me

**PASS**

| Check | Result |
|-------|--------|
| HTTP | 200 |
| Authenticated | yes |
| clientId | present (redacted: `b7ea…b531`) |
| activeActivityId | present (redacted: `fa73…ea6d`) |
| email | present |
| Latency | ~1260 ms |

---

## 4. Activity scope

**PASS**

| Test | HTTP | Expected | Result |
|------|------|----------|--------|
| A. No `X-Activity-Id` (default) | 200 | 200 | PASS |
| B. Authorized `X-Activity-Id` | 200 | 200 | PASS |
| C. Foreign activity (`00000000-0000-0000-0000-000000000099`) | 403 | 403 | PASS |

---

## 5. Chat READ

**PASS** (legacy n8n path)

| Field | Value |
|-------|-------|
| Route | `POST /hcgi/api/chat` |
| Message | `"Quel est mon stock ?"` |
| HTTP | 200 |
| Has reply | yes |
| Reply length | 19 chars (short — monitor) |
| Latency | ~6754 ms |
| 500 errors | none |

V2 flags remain OFF on VPS (H7.2). Legacy `/chat` path confirmed.

---

## 6. History

**PASS**

| Field | Value |
|-------|-------|
| Route | `POST /hcgi/api/history` |
| HTTP | 200 |
| Message count | 56 |
| Foreign data | not observed |
| Latency | ~1010 ms |

---

## 7. Financial READs

**PASS** (legacy `/chat` READ-only)

| Query type | HTTP | Has reply | Latency |
|------------|------|-----------|---------|
| sales | 200 | yes | ~4817 ms |
| expenses | 200 | yes | ~4370 ms |
| products | 200 | yes | ~2821 ms |
| debts | 200 | yes | ~3364 ms |
| report (bénéfice) | 200 | yes | ~4943 ms |

No write intents sent. No 500 responses.

---

## 8. Financial counters

**PARTIAL PASS**

Initial before/after via docker exec failed (Node 20 WebSocket in `@supabase/supabase-js`). Post-smoke snapshot (read-only VPS query with `ws` polyfill):

| Table | Count |
|-------|-------|
| ventes | 0 |
| depenses | 2 |
| stocks | query error (schema/RLS) |
| produits | 0 |
| agent_write_operations | 1 |
| chat_messages | 66 |

**Assessment:** No evidence of financial writes during READ smoke. `ventes`, `produits` at 0; `agent_write_operations` unchanged at 1 (pre-existing). `chat_messages` elevated vs history count — expected chat persistence.

Before/after delta verification: **incomplete** (stocks count failed; no before snapshot).

---

## 9. Chat persistence

**PASS**

| Table | Expected | Observed |
|-------|----------|----------|
| chat_messages | may increase | yes (66 in DB; history 56 — different sources/scope) |
| ventes | unchanged | 0 |
| depenses | unchanged | 2 (pre-existing) |
| stocks | unchanged | not verified |
| produits | unchanged | 0 |
| agent_write_operations | unchanged | 1 (pre-existing) |

---

## 10. Supabase status

**NOT SIMULATED** — `ASH_INTERNAL_HEALTH_KEY` unavailable.

---

## 11. Performance

**PASS with WARNING** (legacy `/chat` READ, V2 OFF)

| Metric | Value |
|--------|-------|
| Samples | 6 (1 stock + 5 financial READs) |
| min | 2821 ms |
| max | 6754 ms |
| mean | 4512 ms |
| **p50** | **4370 ms** |
| **p95** | **6754 ms** |

| Reference | p50 / mean |
|-----------|------------|
| H6 staging outlier | ~7610 ms |
| H7 staging | ~2870–3228 ms |

Production p50 ~4.4 s — between H6 and H7 staging. p95 ~6.8 s warrants monitoring pre-cutover.

Sample latencies (ms): 6754, 4817, 4370, 2821, 3364, 4943

---

## 12. Logs

**NOT re-scanned during this run.** H7.2 baseline: active container (3002) clean; legacy (3001) historical n8n noise.

No auth/scope/500 errors observed in smoke responses.

---

## 13. n8n

**PASS**

| Workflow | Active |
|----------|--------|
| Ash Ledger Bot | yes |
| Ash Ledger Subscription | yes |

No workflows modified. READ-only chat triggered expected n8n paths; no financial write workflows provoked.

---

## 14. Tests

**PASS**

```
npm test (apps/api): 1220 PASS / 0 FAIL / 10 SKIP
```

No regression vs H7.2.

---

## 15. NOT SIMULATED

- Supabase `/api/supabase/status` (no internal health key)
- Financial counters strict before/after (stocks query + missing before snapshot)
- V2 VPS benchmark (V2 remains OFF — correct)

---

## 16. Risks

1. **Performance p95 ~6.8 s** on legacy `/chat` — above H7 staging target.
2. **Stock chat reply short** (19 chars) — verify n8n stock chain quality.
3. **Counter verification incomplete** — stocks table count failed; recommend repeat with fixed query before cutover.
4. **Password in chat** — user pasted credential in conversation; rotate if concerned; never commit.

---

## 17. Final decision

| Decision | **READY_WITH_PERFORMANCE_WARNING** |
|----------|-------------------------------------|
| Auth / me / scope | PASS |
| Chat READ legacy | PASS |
| History | PASS |
| Financial READs | PASS |
| Financial counters | PARTIAL |
| V2 production | OFF |
| F4-B2 production | not executed (correct) |
| Commit | **None** |
| Cutover | **NOT authorized** |

**Next:** Optional H7.3.1 — fix stocks counter query + repeat before/after snapshot. Address p95 before V2 cutover. Do **not** activate V2 until explicit cutover phase.

---

*Generated: H7.3 authenticated smoke — production live, read-only.*
