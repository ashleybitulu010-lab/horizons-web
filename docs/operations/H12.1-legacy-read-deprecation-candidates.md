# H12.1 — Legacy Read Deprecation Candidates

**Date:** 2026-09-17  
**Phase:** H12.1 — CANDIDATE markers only (nothing deleted or disabled)

---

## Status Legend

| Status | Meaning |
|--------|---------|
| **READY_FOR_CONTROLLED_DEPRECATION** | V2 primary for this READ; safe to plan retirement later |
| **FALLBACK** | Still required as safety net |
| **ACTIVE** | Still primary for unmigrated paths |
| **DEAD_CODE_CANDIDATE** | No production consumer found |
| **UNKNOWN** | Insufficient evidence |

---

## After H12.1 SALES Migration

| Component | File / route | Status | Evidence | Future action |
|-----------|--------------|--------|----------|---------------|
| n8n Bot — `Rechercher Ventes` node | n8n workflow `PbTaup5w5sb7tK7W` | **FALLBACK** | V2 primary for sales read; n8n on SAFE_FALLBACK | Keep until zero fallback hits |
| n8n Bot — sales compare nodes | Same workflow | **ACTIVE** | COMPARE not migrated in H12.1 | H12.x later |
| `POST /chat` sales read path | `routes/chat.js` | **FALLBACK** | Frontend routes sales to V2; `/chat` still used on fallback | Keep |
| Legacy agent — `get_sales` tool | `tools/get-sales.js` | **FALLBACK** | V2 uses same tool via plan-executor; Legacy branch if V2 unhandled | Keep |
| Legacy `intent-resolver` sales READ | `intent-resolver/` | **FALLBACK** | Legacy agent branch | Keep |
| `VITE_ASHY_READ_CHAT` global | `ashyReadChatFlag.js` | **ACTIVE** | Other reads still depend on it or n8n | H12.2+ |
| `n8n-history.js` sales context | `utils/n8n-history.js` | **ACTIVE** | History merge unrelated to sales routing | Keep |
| `HomePage.jsx` n8n demo | `src/pages/HomePage.jsx` | **DEAD_CODE_CANDIDATE** | Not routed in App | Future cleanup only |
| `routes/messages.js` | API | **DEAD_CODE_CANDIDATE** | Unmounted | Future cleanup only |

---

## NOT Ready for Deprecation

| Component | Reason |
|-----------|--------|
| Entire Ash Ledger Bot workflow | PDF, writes, fallback, unmigrated reads |
| `POST /chat` route | Fallback + unmigrated intents |
| Legacy agent `index.js` | V2 unhandled + write paths |
| `chatTransport.js` n8n fallback | H4 SAFE_FALLBACK policy |
| PDF routing in `chatRouter.js` | n8n-only |

---

## H12.1 Explicit Non-Candidates

- CREATE_EXPENSE / CREATE_SALE WRITE paths
- PDF generation
- History / thread persistence
- Subscription webhook

**STOP — no deletions in H12.1.**
