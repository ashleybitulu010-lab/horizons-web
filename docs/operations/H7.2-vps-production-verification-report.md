# H7.2 VPS Production Verification

**Date:** 2026-09-16  
**Phase:** H7.2 — VPS access resolution + production verification (NOT cutover)  
**Prior report:** H7.1 NO_GO (SSH + authenticated checks NOT SIMULATED)  
**Constraints honored:** No V2 activation, no flag changes, no nginx/Docker/n8n/Supabase modifications, no financial writes, no commit.

---

## 1. Verdict

**NO_GO**

Infrastructure and flag audit now **PASS**, but authenticated production smoke (auth, scope, chat READ, history, financial counters) and VPS performance remain **NOT SIMULATED**. Cutover readiness cannot be confirmed without these checks.

Progress vs H7.1: SSH resolved; VPS topology, Docker, nginx, flags, and public health verified.

---

## 2. SSH

**PASS**

| Item | Value |
|------|-------|
| Host | `187.124.187.13` |
| User | `root` (project default: `tools/remote-deploy.sh`, `tools/_deploy_frontend_vps.mjs`) |
| Port | `22` |
| H7.1 result | `Permission denied` (default keys / no agent) |
| H7.2 result | `SSH_OK` via deploy key |

**Local SSH audit (no secrets displayed):**

| Variable / file | Status |
|-----------------|--------|
| `VPS_SSH_KEY` env | ABSENT |
| `SSH_AUTH_SOCK` | ABSENT |
| `~/.ssh/config` | ABSENT |
| Default keys (`id_rsa`, `id_ed25519`) | ABSENT |
| Deploy key file | **PRESENT** — basename `ashledger_deploy` under `%TEMP%/ashledger-deploy-key/` |
| Project refs | `tools/remote-deploy.sh` (`VPS_HOST`, `VPS_USER`, `VPS_SSH_KEY`), `.github/workflows/deploy.yml` (self-hosted runner on VPS) |

**Resolution:** H7.1 failed because the project deploy key was not used. H7.2 authenticated with `-i %TEMP%/ashledger-deploy-key/ashledger_deploy`.

---

## 3. VPS topology

**PASS**

| Field | Value |
|-------|-------|
| `whoami` | `root` |
| `hostname` | `srv1866469` |
| Docker | `29.6.2` |
| Docker Compose | `v5.3.1` |
| Frontend root | `/opt/ashledger/dist` |
| Frontend build | `index.html` dated **2026-09-12 00:53 UTC** |
| Deployed git ( `/opt/ashledger` ) | `6097852c1c1919dbe2aa3c0ef344e65a9518752f` |
| Local HEAD | `6097852` (matches deployed commit) |

**Note:** Two API stacks coexist. nginx routes live traffic to the **v2 parallel stack** on port 3002, not the legacy container on 3001.

---

## 4. Docker

**PASS** (with observations)

| Container | Status | Health | Image | Ports | Restart | Created |
|-----------|--------|--------|-------|-------|---------|---------|
| `ash-ledger-api-v2-ash-ledger-api-1` | running | **healthy** | `ash-ledger-api-v2-ash-ledger-api` | `127.0.0.1:3002→3000` | unless-stopped | 2026-09-12 (~4 days) |
| `ash-ledger-api-ash-ledger-api-1` | running | **none** (no healthcheck) | `ash-ledger-api-ash-ledger-api` | `127.0.0.1:3001→3000` | unless-stopped | 2026-08-06 (~5 weeks) |

**Live traffic path:** nginx `/hcgi/api/` → `127.0.0.1:3002` (v2 stack container).

**WARNING:** Legacy container on 3001 remains running but is not proxied by nginx. No action taken (read-only audit).

**Other containers observed:** n8n instances, traefik — not modified.

---

## 5. nginx

**PASS**

| Check | Result |
|-------|--------|
| `nginx -t` | syntax ok, test successful |
| `/` | `root /opt/ashledger/dist`, `try_files` → SPA |
| `/hcgi/api/` | `proxy_pass http://127.0.0.1:3002` |
| `/hcgi/platform/` | `proxy_pass http://127.0.0.1:8090/` |
| `/hcgi/support-bridge/` | `proxy_pass http://127.0.0.1:8791/` |
| Error log (recent) | No critical upstream errors in tail |
| Access log | Health probes return 200 |

No nginx configuration modified. Credential-bearing lines not copied.

---

## 6. Production flags

**PASS**

Script: `tools/_ashy_v2_phase_h7_prod_flag_audit.sh` (executed on VPS via base64 pipe; values never printed).

| Flag | VPS state |
|------|-----------|
| `ASHY_INTELLIGENCE_V2` | ABS (defaults OFF) |
| `ASHY_INTELLIGENCE_V2_HTTP` | ABS (defaults OFF) |
| `ASHY_INTELLIGENCE_V2_PRIMARY` | ABS (defaults OFF) |
| `ASHY_INTELLIGENCE_V2_ACTIONS` | ABS (defaults OFF) |
| `ASHY_INTELLIGENCE_V2_HTTP_CONFIRM` | ABS (defaults OFF) |
| `ASHY_INTELLIGENCE_V2_SHADOW` | ABS (defaults OFF) |
| `ASHY_V2_CUTOVER_MODE` | ABS (defaults OFF) |
| `ASHY_V2_PRIMARY_SAFE_FALLBACK` | ABS (defaults OFF) |

**PRIMARY=ON:** not observed.  
**Unsafe combination:** not observed.  
No corrections applied.

---

## 7. API health

**PASS**

| Endpoint | HTTP | Latency | Summary (redacted) |
|----------|------|---------|-------------------|
| `GET /hcgi/api/health` | 200 | ~1917 ms | `ok: true`, service `ash-ledger-api` |
| `GET /hcgi/api/api/health` | 200 | ~343 ms | `nodeEnv: production`, `supabaseConfigured: true`, `n8nChatConfigured: true`, `internalHealthKeyConfigured: false`, `ashyLlmConfigured: false` |
| `GET /` (frontend) | 200 | — | SPA served (~11 KB) |

---

## 8. PocketBase

**PASS**

| Endpoint | HTTP | Latency |
|----------|------|---------|
| `GET /hcgi/platform/api/health` | 200 | ~293 ms |

Response: `API is healthy` (code 200).

---

## 9. Supabase

**NOT SIMULATED**

`GET /api/supabase/status` requires header `x-ash-internal-key` (`ASH_INTERNAL_HEALTH_KEY`). Production health reports `internalHealthKeyConfigured: false`. No internal key available locally; endpoint not probed. No new key created.

---

## 10. Auth

**NOT SIMULATED**

Authorized production test account flow requires PocketBase login (`ASH_TEST_EMAIL` / `ASH_TEST_PASSWORD`).  
`ASH_TEST_PASSWORD` = **ABSENT** in local environment.  
`GET /hcgi/api/api/me` not executed (would require JWT; not stored or displayed).

---

## 11. Activity scope

**NOT SIMULATED**

Requires authenticated session. Foreign-tenant test skipped (no safe foreign activity ID without auth context).

---

## 12. Chat READ

**NOT SIMULATED**

Legacy READ smoke (e.g. `"Quel est mon stock ?"`) requires authenticated chat session. Not executed to avoid unauthenticated or unauthorized production interaction.

---

## 13. History

**NOT SIMULATED**

`POST /history` requires auth. Not executed.

---

## 14. Financial reads

**NOT SIMULATED**

Authenticated READ queries not executed.

---

## 15. Financial counters

**NOT SIMULATED**

Before/after counts (ventes, depenses, stocks, produits, agent_write_operations) not captured — requires Supabase service access or authenticated API paths not run in this phase.

---

## 16. F4-B2

**PASS (local unit/staging tests only)**

Production F4-B2 replay not executed (no financial writes permitted).

Local API suite includes F4-B2 unit coverage (`agent-session-confirm-f4-b2-*`, staging harnesses skipped without `RUN_STAGING`). No production regression signal from VPS logs on active container (v2 stack: no matching errors in filtered tail).

---

## 17. Fallback

**PASS (inferred from flags + code defaults)**

All V2/PRIMARY/SHADOW flags ABS on VPS → legacy path expected for chat. Production fallback behavior not live-tested (depends on auth smoke).

---

## 18. n8n

**PASS (read-only MCP audit)**

| Workflow | ID | Active |
|----------|-----|--------|
| Ash Ledger Bot | `PbTaup5w5sb7tK7W` | yes |
| Ash Ledger Subscription | `nUVQK4JJDkbFPtSi` | yes |

No workflows modified. No financial execution triggered.

**Log observation (legacy container 3001, not in nginx path):** historical `chat error: Réponse n8n vide (200)` entries and one `unhandled SyntaxError: Bad escaped character in JSON`. Active v2 container (3002) filtered log tail: clean. Monitor if legacy container should be decommissioned post-cutover planning.

---

## 19. Performance

**NOT SIMULATED**

V2 intelligence flags remain OFF. No safe VPS-internal V2 benchmark path identified that avoids enabling production V2 traffic. Staging baseline (H7.1 reference): p50 ~3228 ms, mean ~3183 ms (5 samples). H6 reference: ~7610 ms.

Public health latency (unauthenticated): `/hcgi/api/health` ~1917 ms (cold/warm unknown); `/hcgi/api/api/health` ~343 ms.

---

## 20. Logs

**PARTIAL PASS**

| Source | Finding |
|--------|---------|
| `ash-ledger-api-v2-*` (live) | No ERROR/unhandled in filtered tail |
| `ash-ledger-api-*` (legacy, idle) | Repeated n8n empty-response chat errors (historical) |
| nginx error log | No recent critical upstream failures |
| Secrets in logs | None displayed |

---

## 21. Tests

**PASS**

```
npm test (apps/api): 1220 PASS / 0 FAIL / 10 SKIP
Duration: ~41 s
```

| Comparison | H7 baseline | H7.2 |
|------------|-------------|------|
| Pass | 1201 | 1220 (+19 new tests) |
| Fail | 0 | 0 |
| Skip | 10 | 10 |

Lint (`eslint src/agent/intelligence-v2 --quiet`): **PASS**

No new regressions observed. Local H7/H7.1 intelligence-v2 test files present but uncommitted.

---

## 22. Risks

1. **Authenticated production path unverified** — auth, scope, chat, history, counters still unknown on live system.
2. **Dual API stacks** — nginx uses v2 parallel container (3002); legacy (3001) still running without healthcheck; naming may confuse cutover planning.
3. **Legacy container log noise** — n8n empty-response errors on idle stack; confirm decommission plan.
4. **Supabase internal health key unset** — `/api/supabase/status` unavailable for ops probing.
5. **Performance unmeasured** — no authenticated READ latency baseline on VPS production path.

---

## 23. NOT SIMULATED

- Supabase `/api/supabase/status` (no internal key)
- Auth (`/api/me`)
- Activity scope (default / own / foreign)
- Chat READ legacy smoke
- History
- Financial counters before/after
- V2 VPS performance benchmark
- Production F4-B2 live replay

---

## 24. Final decision

| Decision | **NO_GO** |
|----------|-----------|
| Rationale | SSH and infrastructure checks now pass, V2 flags confirmed OFF, but H7.1 blockers for authenticated production verification remain open. |
| vs H7.1 | Improved: SSH PASS, flags verified, Docker/nginx/health documented. Unchanged: auth/chat/history/counters/perf NOT SIMULATED. |
| Cutover | **NOT authorized** — H7.2 is verification only. |
| Commit | **None** |

**Next step to reach READY_FOR_CUTOVER:** Provide authorized test credentials via secure env (`ASH_TEST_PASSWORD` for existing test account) and re-run steps 10–17 without activating V2 flags.

---

*Generated: Phase H7.2 — read-only production verification.*
