# H12.4 — PRODUCTS READ MIGRATION

**Date:** 2026-09-18  
**Prior:** H12.3 `H12.3_GO` @ `406d744`  
**Verdict:** `H12.4_GO_WITH_GAPS`

---

## 1. Verdict

**H12.4_GO_WITH_GAPS** — Products catalog READ migré vers Ashy V2 via frontend. Tous les critères GO critiques satisfaits. Gap documenté : requêtes prix/détail produit unique (non classifiées V2).

---

## 2. Audit

Voir `docs/operations/H12.4-products-read-audit.md`.

Backend V2 déjà opérationnel pour catalog READ (`get_products`, classifier, template). Aucune modification backend requise.

---

## 3. Avant / après routing

**Avant (VITE_ASHY_READ_PRODUCTS=false):**
```
"Liste mes produits" → write guards → n8n (Legacy)
```

**Après (VITE_ASHY_READ_PRODUCTS=true):**
```
"Liste mes produits" → write guards → isMigratedReadIntent(PRODUCTS) → ASHY → V2 HTTP
```

Ordre respecté : WRITE guards → Sales → Expenses → Stock → **Products** → global read → n8n.

---

## 4. Feature flag

| Flag | Production | Default repo |
|------|------------|--------------|
| `VITE_ASHY_READ_PRODUCTS` | **`true`** | `false` |
| `VITE_ASHY_READ_SALES` | `true` | ON |
| `VITE_ASHY_READ_EXPENSES` | `true` | OFF (enabled in prod deploy) |
| `VITE_ASHY_READ_STOCK` | `true` | OFF (enabled in prod deploy) |

Rollback : `VITE_ASHY_READ_PRODUCTS=false` → `node tools/_h124_deploy_frontend.mjs`

---

## 5. Fichiers modifiés

### Frontend
- `src/lib/ashyReadMigration.js` — `READ_CAPABILITY.PRODUCTS`, flag, patterns, write exclusions
- `src/lib/chatRouter.js` — hook PRODUCTS after Stock
- `src/lib/ashyReadMigration.test.js` — tests H12.4
- `.env.example` — `VITE_ASHY_READ_PRODUCTS=false`
- `tools/_h124_deploy_frontend.mjs`
- `tools/_h124_products_read_migration_gate.mjs`
- `docs/operations/H12.4-products-read-audit.md`
- `docs/operations/H12.4-products-read-migration.md`

### Backend
Aucun changement.

---

## 6. Tests frontend

```
node --test src/lib/ashyReadMigration.test.js src/lib/chatRouter.test.js
→ 60 pass / 0 fail
```

Couverture H12.4 :
- READ positifs (catalogue)
- WRITE safety (ajoute/crée/modifie/supprime produit)
- Stock regression
- Flag OFF → n8n
- Price gaps → n8n (non migrés)

---

## 7. Tests backend

Aucun changement. Tests existants H7.8/H12.3 passent (classifier products catalog + template).

---

## 8. Production smoke

Gate : `node tools/_h124_products_read_migration_gate.mjs`  
Tag : `h124-1789687961974`  
Bundle : `assets/index-z-cfONet.js`

| Scenario | Status | Domain | Outcome |
|----------|--------|--------|---------|
| Liste mes produits | 200 | PRODUCTS | SUCCESS |
| Montre-moi mes produits | 200 | PRODUCTS | SUCCESS |
| Quels produits ai-je ? | 200 | PRODUCTS | SUCCESS |
| Quelles sont mes ventes ? | 200 | SALES | SUCCESS |
| Quelles sont mes dépenses ? | 200 | EXPENSES | SUCCESS |
| Combien me reste-t-il de poulets ? | 200 | STOCK | SUCCESS |
| Foreign activity | 403 | — | PASS |

Catalogue vide → réponse NO_DATA légitime (SUCCESS avec message « aucun produit enregistré »).

---

## 9. Metrics BEFORE/AFTER

| Metric | Delta |
|--------|-------|
| READ_TOTAL | +6 |
| READ_V2_TOTAL | +6 |
| PRODUCTS.READ_V2_SUCCESS | +3 |
| SALES.READ_V2_SUCCESS | +1 |
| EXPENSES.READ_V2_SUCCESS | +1 |
| STOCK.READ_V2_SUCCESS | +1 |
| ACTION_TO_N8N_FALLBACK | 0 |

Correlation IDs : `rr-0c715b03-27b`, `rr-723fc87f-dd0`, `rr-70b0f29a-c91`

---

## 10. Financial delta

| Table | Δ |
|-------|---|
| ventes | 0 |
| depenses | 0 |
| stocks | 0 |
| produits | 0 |
| agent_write_operations | 0 |

---

## 11. Product table delta

**produits Δ0** — aucune écriture produit déclenchée.

---

## 12. Security/scope

- Activité propre → 200
- Activité étrangère (`00000000-…-0099`) → 403
- Scope via PocketBase auth → client → activity (inchangé)

---

## 13. Write safety

Classification/routing local vérifié (pas d'exécution write en prod) :
- « Ajoute un produit » → WRITE → n8n
- « Modifie le prix du poulet » → WRITE → n8n
- « Supprime le produit poulet » → WRITE → n8n

Aucun produit modifié en production.

---

## 14. n8n/Legacy status

n8n et Legacy **intacts**. Debts, Profit, PDF restent sur n8n. Product WRITE reste Legacy/ACTION.

---

## 15. Rollback

Local : `VITE_ASHY_READ_PRODUCTS=false` → « Liste mes produits » → n8n (tests).  
Prod : `node tools/_h124_deploy_frontend.mjs` avec flag false.

---

## 16. Git

Commit : `feat(operations): migrate products read to Ashy V2 (H12.4)`

---

## 17. Known gaps

| Gap | Impact | Next step |
|-----|--------|-----------|
| « Quel est le prix du poulet ? » | V2 classifier `null` → Legacy | H12.x+ ou classifier extension |
| « Combien coûte mon poulet ? » | idem | idem |
| « Montre-moi le poulet » | idem | idem |
| « Change le prix du produit » | WRITE guard partiel (reste n8n) | Améliorer write guard si besoin |

---

## 18. Next step

**STOP** — Attendre validation humaine avant H12.5 (Debts). Ne pas migrer Debts/Profit/PDF/Product WRITE automatiquement.
