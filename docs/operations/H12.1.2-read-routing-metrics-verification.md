# H12.1.2 — READ Routing Metrics Endpoint Verification

**Date:** 2026-09-18  
**Phase:** H12.1.2 — Metrics endpoint verification only (no migration, no runtime change)  
**Prior:** H12.1.1 `H12.1.1_GO_WITH_GAPS` @ `cf2f744`  
**Tag:** `h1212-1789684234669`  
**Artifact:** `%TEMP%/h1212_report.json`  
**Gate script:** `tools/_h1212_read_routing_metrics_gate.mjs`  
**Verdict:** **H12.1.2_GO_WITH_GAPS**  
**Provisioning (2026-09-18):** **KEY_PROVISIONED = YES** | **METRICS_ENDPOINT = VERIFIED**  
**Gate tag (post-provision):** `h1212-1789684907147`

---

## 1. Objective

Close H12.1.1 observability gaps:

1. Verify `GET /api/read-routing/metrics` with authorized internal key
2. Verify counters match observed READ routing

**Verification only.** No routing migration, no flag changes, no writes.

---

## 2. Endpoint

| Item | Value |
|------|-------|
| Route (in-app) | `GET /api/read-routing/metrics` |
| Public URL | `GET /hcgi/api/api/read-routing/metrics` |
| Router | `apps/api/src/routes/api/index.js` |
| Handler | `read-routing-metrics.js` → `getReadRoutingMetricsSnapshot()` |
| Middleware | `requireInternalHealthKey` |
| Auth header | `x-ash-internal-key` (also `X-Ash-Internal-Key`) |
| Env var | `ASH_INTERNAL_HEALTH_KEY` |
| Response shape | `{ global: {...}, byCapability: {...}, timestamp: ISO }` |

### Global metrics (when exposed)

`READ_TOTAL`, `READ_V2_TOTAL`, `READ_LEGACY_TOTAL`, `READ_N8N_FALLBACK_TOTAL`, `READ_V2_ERROR_TOTAL`, `READ_SAFE_FALLBACK_TOTAL`, `ACTION_TO_N8N_FALLBACK`

### Per-capability outcomes (when populated)

`READ_V2_SUCCESS`, `READ_V2_ERROR`, `READ_SAFE_FALLBACK`, `READ_LEGACY`, `READ_N8N_FALLBACK` + `latency` stats

Capabilities: SALES, EXPENSES, STOCK, PRODUCTS, DEBTS, PROFIT, REPORT, HISTORY (appear only after at least one event)

---

## 3. Security

### Pre-provision (2026-09-18 initial gate)

| Test | Expected | Observed | Pass |
|------|----------|----------|------|
| A — public, no key | 401/403 | **503** | **FAIL** |
| B — public, wrong key | 401/403 | **503** | **FAIL** |
| C — loopback, good key | 200 | **503** | **FAIL** |

Cause: `ASH_INTERNAL_HEALTH_KEY` absent from container env.

### Post-provision (final)

| Test | Expected | Observed | Pass |
|------|----------|----------|------|
| A — public, no key | 401/403 | **401** | **PASS** |
| B — public, wrong key | 401/403 | **401** | **PASS** |
| C — loopback, good key | 200 | **200** | **PASS** |
| Metrics leaked without auth | no | no | **PASS** |
| Password in auth response | no | no | **PASS** |
| Key in Git / reports / logs | no | no | **PASS** |

**No metrics or secrets exposed.** Unauthorized access returns `{ "error": "Unauthorized" }`.

---

## 4. Loopback

| Check | Result |
|-------|--------|
| Container | `ash-ledger-api-v2-ash-ledger-api-1` (recreated, **healthy**) |
| `ASH_INTERNAL_HEALTH_KEY` in container env | **SET** (value not recorded) |
| Provisioning target | `/docker/ash-ledger-api-v2/.env` (runtime only, not Git) |
| Generation | `openssl rand -base64 48` (cryptographic) |
| Loopback port | **3000** (`process.env.PORT`) |
| Loopback path | `/api/read-routing/metrics` |
| HTTP with env key | **200** |
| Public `/api/health` | **200** |

---

## 5. Metrics exposed

| Source | Result |
|--------|--------|
| HTTP snapshot | **VERIFIED** |
| Global counters via API | **EXPOSED** (all 7 global metrics) |
| Capability counters via API | **EXPOSED after events** (SALES, EXPENSES, STOCK after smoke) |
| Unused capabilities | **NOT_EXPOSED** until instrumented (PRODUCTS, DEBTS, PROFIT, REPORT, HISTORY) |

---

## 6. Before metrics

Post-provision baseline (fresh container):

| Global | Value |
|--------|-------|
| READ_TOTAL | 0 |
| READ_V2_TOTAL | 0 |
| ACTION_TO_N8N_FALLBACK | 0 |
| byCapability | `{}` |

---

## 7. Controlled READs

Post-provision gate (`h1212-1789684907147`):

| Capability | HTTP | Latency ms | Correlation | V2 domain |
|------------|------|------------|-------------|-----------|
| SALES | 200 | 1452 | rr-9c0637c4-0c6 | SALES / SUCCESS |
| EXPENSES | 200 | 757 | rr-4fa619e2-2da | EXPENSES / SUCCESS |
| STOCK | 200 | 704 | rr-ff7384d8-3d9 | STOCK / SUCCESS |

All via `POST /api/ashy/chat` (READ-only). No ACTION. Frontend routing unchanged.

---

## 8. After metrics

| Global | Value |
|--------|-------|
| READ_TOTAL | 3 |
| READ_V2_TOTAL | 3 |
| ACTION_TO_N8N_FALLBACK | 0 |

| Capability | READ_V2_SUCCESS | latency p50 |
|------------|-----------------|-------------|
| SALES | 1 | 439 ms |
| EXPENSES | 1 | 286 ms |
| STOCK | 1 | 274 ms |

---

## 9. Counter deltas

| Metric | Delta | Expected | Coherent |
|--------|-------|----------|----------|
| READ_TOTAL | **+3** | +3 (1 per READ) | **YES** |
| READ_V2_TOTAL | **+3** | +3 | **YES** |
| SALES READ_V2_SUCCESS | **+1** | +1 | **YES** |
| EXPENSES READ_V2_SUCCESS | **+1** | +1 | **YES** |
| STOCK READ_V2_SUCCESS | **+1** | +1 | **YES** |
| ACTION_TO_N8N_FALLBACK | **0** | 0 | **YES** |

**EXPECTED_INTERNAL_MULTIPLIER:** 1 (one counter increment per successful V2 READ request).

---

## 10. Correlation

| Check | Result |
|-------|--------|
| `X-Ash-Read-Correlation` on Sales READ | **PASS** — `rr-9c0637c4-0c6` |
| Metrics reflect same READ window | **PASS** — SALES `READ_V2_SUCCESS` +1 after sales smoke |
| `docker logs` grep for correlation id | **NOT_FOUND** (gap) |

Header + metrics correlation **verified**. Docker log grep remains a minor observability gap (morgan/combined format).

---

## 11. Fallback

**SAFE_FALLBACK = NOT_SIMULATED** (acceptable — no intentional V2 break).

---

## 12. ACTION safety

| Check | Result |
|-------|--------|
| `ACTION_TO_N8N_FALLBACK` before | **0** |
| `ACTION_TO_N8N_FALLBACK` after | **0** |
| ACTION test performed | **none** |
| Code invariant | unchanged — ACTION blocked in `recordReadRoutingEvent` |

---

## 13. Financial delta

| Table | Delta |
|-------|-------|
| ventes | 0 |
| depenses | 0 |
| stocks | 0 |
| produits | 0 |
| agent_write_operations | 0 |

**PASS** — financial integrity preserved.

---

## 14. Performance

| READ | Latency ms |
|------|------------|
| Sales | 940 |
| Expenses | 798 |
| Stock | 700 |

Informational only (3 samples). No regression conclusion.

---

## 15. Tests

| Suite | Result |
|-------|--------|
| `read-routing-observability.test.js` | **10/0 PASS** |
| H12.1.2 gate (post-provision) | security + metrics + counters **PASS** |

No application runtime code modified. Configuration-only change on VPS.

---

## 16. Gaps remaining

| Gap | Status |
|-----|--------|
| H12.1.1 `METRICS_ENDPOINT` | **CLOSED** |
| H12.1.1 `SALES_V2_COUNTER` via API | **CLOSED** |
| Log correlation via `docker logs` grep | **OPEN** (minor) |
| Unused capability buckets until events | **EXPECTED** |

---

## 17. Verdict

### **H12.1.2_GO_WITH_GAPS**

| Criterion | Result |
|-----------|--------|
| Key provisioned (runtime only) | **PASS** |
| Endpoint 200 with good key | **PASS** |
| Bad/missing key rejected (401) | **PASS** |
| Metrics readable | **PASS** |
| Counters coherent (+3/+1/+1/+1) | **PASS** |
| Correlation | **PARTIAL** — header + metrics; log grep gap |
| Financial delta 0 | **PASS** |
| ACTION→n8n = 0 | **PASS** |
| No secrets exposed | **PASS** |
| Flags unchanged | **PASS** (V2/HTTP/PRIMARY/ACTIONS/CONFIRM ON, SHADOW OFF) |

---

## Provisioning summary

| Item | Value |
|------|-------|
| KEY_PROVISIONED | **YES** |
| METRICS_ENDPOINT | **VERIFIED** |
| Method | `/docker/ash-ledger-api-v2/.env` + container recreate |
| Key in Git | **NO** |
| Key in reports | **NO** |

---

## STOP

- H12.2 **not launched**
- Expenses **not migrated**
- n8n / Legacy **unchanged**
- Supabase / flags / WRITE paths **unchanged**
